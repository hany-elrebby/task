server:
    port: 8080

spring:
    application:
        name: task

    datasource:
        url: jdbc:postgresql://localhost:5432/task
        username: postgres
        password: postgres

    jpa:
        hibernate:
            ddl-auto: update